import http from'node:http';import{readFile}from'node:fs/promises';import{resolve,extname}from'node:path';import{randomBytes}from'node:crypto';import{handle}from'./lib/commerce.mjs';process.env.CART_SIGNING_KEY||=randomBytes(32).toString('hex');const root=resolve('public'),port=8771;http.createServer(async(req,res)=>{try{const url=new URL(req.url,'http://127.0.0.1:'+port);if(url.pathname.startsWith('/api/')){let b=[];for await(const c of req)b.push(c);const r=await handle(new Request(url,{method:req.method,headers:req.headers,...(req.method==='POST'?{body:Buffer.concat(b)}:{})}));res.writeHead(r.status,Object.fromEntries(r.headers));res.end(Buffer.from(await r.arrayBuffer()));return}let path=resolve(root,'.'+decodeURIComponent(url.pathname));if(!path.startsWith(root+'/')&&path!==root){res.writeHead(403);res.end();return}if(!extname(path))path=resolve(path,'index.html');const data=await readFile(path);const types={'.html':'text/html','.js':'application/javascript','.css':'text/css','.jpg':'image/jpeg','.png':'image/png','.csv':'text/csv','.md':'text/plain','.zip':'application/zip'};res.writeHead(200,{'Content-Type':types[extname(path)]||'application/octet-stream'});res.end(data)}catch{res.writeHead(404);res.end('Not found')}}).listen(port,'127.0.0.1',()=>console.log('http://127.0.0.1:'+port));
